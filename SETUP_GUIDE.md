# 🚀 خطوات تشغيل المشروع مع phpMyAdmin

## المتطلبات الأساسية
- ✅ Node.js مثبت
- ✅ XAMPP مثبت (يحتوي على phpMyAdmin + MySQL/MariaDB)
- ✅ قاعدة البيانات `ai_db` موجودة بالفعل

---

## 💾 الخطوة 1: إضافة بيانات Admin في phpMyAdmin

### أ) شغل XAMPP
- افتح XAMPP Control Panel
- اضغط Start لـ Apache
- اضغط Start لـ MySQL

### ب) افتح phpMyAdmin
- افتح المتصفح وروح: `http://localhost/phpmyadmin`

### ج) اختر قاعدة البيانات
- اضغط على `ai_db` من القائمة اليسار

### د) أضف بيانات Admin
- اضغط على تبويب **SQL** في الأعلى
- انسخ والصق هذا الكود:

```sql
-- إضافة admin جديد
INSERT INTO administrator (Username, Email, Password) VALUES
('Admin User', 'admin@example.com', 'admin123');

-- أو استخدم بياناتك الخاصة
INSERT INTO administrator (Username, Email, Password) VALUES
('Your Name', 'your.email@gmail.com', 'your_password');

-- تأكد من البيانات
SELECT * FROM administrator;
```

- اضغط **Go** أو **تنفيذ**

### هـ) تأكد من إضافة البيانات
- اضغط على جدول `administrator` من القائمة
- اضغط **Browse** أو **عرض**
- يجب تشوف البيانات اللي أضفتها

---

## ⚙️ الخطوة 2: تعديل إعدادات الاتصال بقاعدة البيانات

افتح الملف: `backend/config/database.js`

تأكد من الإعدادات:
```javascript
host: 'localhost',
user: 'root',           // عادة root في XAMPP
password: '',           // فاضي في XAMPP (default)
database: 'ai_db',      // اسم قاعدة البيانات
```

**ملاحظة:** في XAMPP، كلمة المرور عادة تكون فاضية (empty string)

---

## 🔧 الخطوة 3: تثبيت Dependencies

### للباك إند:
```bash
cd backend
npm install
```

إذا ما عندك `package-lock.json` في مجلد backend، المكتبات المطلوبة:
- express
- cors
- body-parser
- mysql2

### للفرونت إند:
```bash
cd ..
npm install
```

---

## ▶️ الخطوة 4: تشغيل المشروع

### 1. تأكد XAMPP MySQL شغال
في XAMPP Control Panel، يجب MySQL يكون **Running** (أخضر)

### 2. شغل الباك إند (terminal أول):
```bash
cd backend
npm start
```

يجب تشوف:
```
✅ Database connected successfully
🚀 Server is running on port 5000
```

إذا شفت خطأ في الاتصال بقاعدة البيانات، تأكد من:
- XAMPP MySQL شغال
- اسم قاعدة البيانات `ai_db` موجود
- username و password صحيحين في `database.js`

### 3. شغل الفرونت إند (terminal ثاني):
```bash
npm run dev
```

يجب تشوف: `Local: http://localhost:5174/`

---

## 🔐 بيانات تسجيل الدخول

استخدم البيانات اللي أضفتها في phpMyAdmin، مثلاً:

**مثال:**
- Email: `admin@example.com`
- Password: `admin123`

---

## ➕ إضافة Admin جديد

### طريقة 1: من phpMyAdmin
1. افتح phpMyAdmin
2. اختر قاعدة البيانات `ai_db`
3. اختر جدول `administrator`
4. اضغط **Insert** أو **إدراج**
5. املأ الحقول:
   - Username: اسمك
   - Email: بريدك
   - Password: كلمة المرور
6. اضغط **Go** أو **تنفيذ**

### طريقة 2: من SQL
في phpMyAdmin → SQL:
```sql
INSERT INTO administrator (Username, Email, Password) 
VALUES ('اسمك', 'email@example.com', 'كلمة_المرور');
```

---

## 🐛 حل المشاكل الشائعة

### ❌ Backend لا يتصل بقاعدة البيانات
**الحل:**
1. تأكد XAMPP MySQL شغال (أخضر)
2. تأكد من اسم قاعدة البيانات: `ai_db`
3. تأكد user: `root` و password: `''` (فاضي)
4. جرب restart لـ MySQL في XAMPP

### ❌ Table 'ai_db.administrator' doesn't exist
**الحل:**
- قاعدة البيانات موجودة بس الجدول ناقص
- راجع الـ SQL dump اللي عندك وتأكد جدول administrator موجود

### ❌ Cannot find module 'mysql2'
**الحل:**
```bash
cd backend
npm install mysql2
```

### ❌ Port 5000 already in use
**الحل:**
- في ملف `backend/server.js` غير PORT من 5000 لـ 5001
- أو أوقف البرنامج اللي يستخدم port 5000

### ❌ Login failed - Invalid credentials
**الحل:**
1. افتح phpMyAdmin
2. تأكد من البيانات في جدول `administrator`
3. تأكد Email و Password مطابقين بالضبط

---

## 📊 هيكل قاعدة البيانات الموجودة

عندك الجداول التالية:
- ✅ `administrator` - بيانات المسؤولين
- ✅ `registereduser` - المستخدمين المسجلين
- ✅ `content` - المحتوى
- ✅ `subscription` - الاشتراكات
- ✅ `payment` - المدفوعات
- ✅ `complaintssuggestions` - الشكاوى والاقتراحات

---

## 📝 ملاحظات مهمة

⚠️ **تحذير أمني**:
- المشروع للتطوير فقط (Development)
- كلمات المرور مخزنة كنص عادي (Plain Text)
- للإنتاج (Production) يجب:
  - استخدام `bcrypt` لتشفير كلمات المرور
  - إضافة JWT tokens للـ authentication
  - استخدام HTTPS
  - إضافة validation للمدخلات

---

## ✅ الخطوات باختصار

1. ✅ شغل XAMPP (Apache + MySQL)
2. ✅ افتح phpMyAdmin → `ai_db`
3. ✅ أضف admin في جدول `administrator`
4. ✅ تأكد من إعدادات `backend/config/database.js`
5. ✅ `cd backend && npm install && npm start`
6. ✅ `npm run dev` (في مجلد المشروع الرئيسي)
7. ✅ سجل دخول بالبيانات اللي أضفتها

---

## 🎉 بالتوفيق!

إذا واجهت أي مشكلة، تأكد من:
1. ✅ XAMPP MySQL شغال (أخضر)
2. ✅ قاعدة البيانات `ai_db` موجودة
3. ✅ جدول `administrator` فيه بيانات
4. ✅ Backend شغال على port 5000
5. ✅ Frontend شغال على port 5174
